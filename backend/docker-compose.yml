services:
  backend:
    build:
      context: .
      dockerfile: backend.dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    # env vars are loaded from .env — see README for required keys:
    # SUPABASE_URL, SUPABASE_SERVICE_KEY, MODEL_API_KEY, MODEL_NAME
